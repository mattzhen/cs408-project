<div class="table-responsive">
  <table class="table table-striped table-hover">
    <thead>
      <tr>
        <th>Item Name</th>
        <th>Quantity</th>
        <th>Price Per Unit</th>
        <th>Obtained</th>
        <th>More info</th>
      </tr>
    </thead>
    <tbody>
      <% groceries.forEach((item) => { %>
        <tr>
          <td><%= item.item_name %></td>
          <td><%= item.quantity %></td>
          <td><%= item.price ? '$' + item.price.toFixed(2) : 'N/A' %></td>
          <td><%= item.was_obtained ? 'Yes' : 'No' %></td>
          <td>
            <a href="/item-details/<%= item.item_id %>" class="btn btn-primary btn-sm">View More</a>
          </td>
        </tr>
      <% }) %>
    </tbody>
  </table>
</div>
<a href="/add-item" class="btn btn-secondary my-3">Add more items</a>

<% if (hasNull) { %>
  <p id="nullWarning" class="text-warning">* Some items are missing a price and are not included in the total.</p>
<% } %>

<br>
<p id="totalCost" class="mb-0 fs-5">Total: <%= total ? '$' + total.toFixed(2) : '$0.00' %></p>
<div class="d-flex align-items-center gap-2">
  <p class="mb-0 fs-5">Done shopping?</p>
  <form action="/delete-obtained" method="POST" onsubmit="return confirm('Are you sure?')">
    <button type="submit" class="btn btn-danger">Delete Obtained Items</button>
  </form>
</div>